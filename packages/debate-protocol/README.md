# debate-protocol

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build debate-protocol` to build the library.

## Running unit tests

Run `nx test debate-protocol` to execute the unit tests via [Vitest](https://vitest.dev/).
