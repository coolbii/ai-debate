# prompt-spec

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build prompt-spec` to build the library.

## Running unit tests

Run `nx test prompt-spec` to execute the unit tests via [Vitest](https://vitest.dev/).
